import 'package:flutter/material.dart';

class TaskGroupWidget extends StatelessWidget {
  const TaskGroupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
